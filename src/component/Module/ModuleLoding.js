import React, {Component} from 'react';
import PropTypes from 'prop-types';

class ModuleLoding extends Component{
    render(){
        return(
            <div>

            </div>
        )
    }
}
ModuleLoding.propTypes = {
    name: PropTypes.string.isRequired
}
export default ModuleLoding;